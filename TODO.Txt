Next Release
============

- Improve system tray icon tooltip, when clocked in.

- Fix Session Editing dialog to not repaint so badly (in place updates instead of complete refresh).

- Export a range of dates only.

- Save using .tmp file and add .bak file.

- Persist font configured for window report.

- Allow session pruning using a range.

- Report options: day only (not session), overall total, ignore empty days.


Later Release
=============

- XML format import/export.

- Add Holidays option. Enter date range/time and Task name.

- Other report formats, e.g. HTML & XML (File & Clipboard)

- Change report dialogs to use a common one.
